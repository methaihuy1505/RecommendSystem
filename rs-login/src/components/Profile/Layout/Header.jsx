import React from "react";

const Header = () => {
  return (
    <header className="header d-flex justify-content-end align-items-center px-3 py-2">
      <button>Sign out</button>
    </header>
  );
};

export default Header;
